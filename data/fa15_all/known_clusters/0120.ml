TupleG (fromList [LitG,LetG NonRec (fromList [EmptyG]) EmptyG])
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
